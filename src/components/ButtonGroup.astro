---
import { cn } from '@/utils'

interface Props {
  class?: string
}

const props = Astro.props
---

<div
  class={cn(
    "inline-flex [&>button:not(:last-child)]:rounded-tr-none [&>button:not(:last-child)]:rounded-br-none [&>button:not(:first-child)]:rounded-tl-none [&>button:not(:first-child)]:rounded-bl-none [&>button]:ml-[-1px] [&>button:first-child]:ml-0",
    props.class
  )}
>
  <slot />
</div>
