---
import { joinIfArray } from '@/utils/utils'

interface Props {
  value: string | string[] | number | number[]
  separator?: string
}

const props: Props = Astro.props
---

{joinIfArray(props.value, props.separator)}
