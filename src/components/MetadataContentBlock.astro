---
import { cn } from "@/utils";

interface Props {
  class?: string;
}

const props: Props = Astro.props;
---

<div
  class={cn(
    "p-4 my-4 border-s-4 rounded-md border-gray-300 bg-gray-100 dark:border-gray-500 dark:bg-gray-800 text-[var(--foreground)]",
    props.class
  )}
>
  <slot />
</div>
